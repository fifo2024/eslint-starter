interface WebStorageOptions<T = any, K extends string = string> {
    storage: 'localStorage' | 'sessionStorage';
    key: K;
    initialValue?: T | (() => T);
}
type onChange<T> = (newValue: T, oldValue: T | null) => void;
type onRemove<T, K> = (key: K, oldValue: T | null) => void;
declare class WebStorage<T = any, K extends string = string> {
    private storage?;
    private onChanges;
    private onRemoves;
    key: K;
    constructor(props: WebStorageOptions<T, K>);
    onChange: (fn: onChange<T>) => number;
    onRemove: (fn: onRemove<T, K>) => number;
    get: () => T | null;
    set: (value: T) => void;
    remove: () => void;
}

export { type WebStorageOptions, WebStorage as default };
